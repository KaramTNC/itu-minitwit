name: Smoke Deploy

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "docker/**"
      - "docker-compose.yml"
      - "docker-stack.yml"
      - "prometheus.yml"
      - "grafana/**"

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0

      - name: Log in to DockerHub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Build & Push Web image
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: .
          file: docker/web/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/itu-web-server:staging
            ${{ secrets.DOCKER_USERNAME }}/itu-web-server:${{ github.sha }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

  build-api:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0

      - name: Log in to DockerHub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Build & Push API image
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: .
          file: docker/api/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/itu-api-server:staging
            ${{ secrets.DOCKER_USERNAME }}/itu-api-server:${{ github.sha }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

  deploy:
    runs-on: ubuntu-latest
    needs: [build-web, build-api]
    env:
      DB_URL: ${{ secrets.DB_URL }}
      GF_SECURITY_ADMIN_USER: ${{ secrets.GF_SECURITY_ADMIN_USER }}
      GF_SECURITY_ADMIN_PASSWORD: ${{ secrets.GF_SECURITY_ADMIN_PASSWORD }}
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh/
          echo "$SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}

      - name: Setup .NET
        uses: actions/setup-dotnet@55ec9447dda3d1cf6bd587150f3262f30ee10815 # v3.4.2
        with:
          dotnet-version: 8.0.x

      - name: Build database migration bundle
        run: |
          dotnet tool install --global dotnet-ef --version 8.0.22
          dotnet restore src/Web/Web.csproj
          dotnet ef migrations bundle --self-contained --runtime linux-x64 --configuration Release --project src/Infrastructure --startup-project src/Web --output ./efbundle

      - name: Copy files to staging server
        run: |
          rsync -az --checksum -e "ssh -o StrictHostKeyChecking=no" docker-compose.yml docker-stack.yml prometheus.yml grafana/ efbundle root@134.122.71.241:/itu-minitwit/

      - name: Create .env file on staging server
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 "
            echo 'DB_URL=${{ secrets.DB_URL }}' > /itu-minitwit/.env
          "

      - name: Apply database migrations on staging server
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 "
            cd /itu-minitwit &&
            chmod +x ./efbundle &&
            timeout 60 ./efbundle --connection '${{ secrets.DB_URL }}'
          "

      - name: Ensure Docker Swarm is active
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 "
            swarm_state=\$(docker info --format '{{.Swarm.LocalNodeState}}') &&
            control_available=\$(docker info --format '{{.Swarm.ControlAvailable}}') &&
            if [ \"\$swarm_state\" != 'active' ]; then
              docker swarm init --advertise-addr 134.122.71.241
            elif [ \"\$control_available\" != 'true' ]; then
              docker swarm leave --force &&
              docker swarm init --advertise-addr 134.122.71.241
            fi
          "

      - name: Create Docker secrets on staging server
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 "
            if ! docker secret inspect itu_grafana_admin_user >/dev/null 2>&1; then
              printf '%s' '${{ secrets.GF_SECURITY_ADMIN_USER }}' | docker secret create itu_grafana_admin_user -
            fi
            if ! docker secret inspect itu_grafana_admin_password >/dev/null 2>&1; then
              printf '%s' '${{ secrets.GF_SECURITY_ADMIN_PASSWORD }}' | docker secret create itu_grafana_admin_password -
            fi
          "

      - name: Rolling deploy on staging
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 'bash -s' <<'REMOTE'
          set -euo pipefail

          diagnose_service() {
            service="$1"
            echo "::group::${service} tasks"
            docker service ps --no-trunc "$service" || true
            echo "::endgroup::"
            echo "::group::${service} recent logs"
            docker service logs --tail 80 "$service" || true
            echo "::endgroup::"
          }

          wait_service() {
            service="$1"
            expected="$2"

            for attempt in $(seq 1 36); do
              replicas=$(docker service ls --filter "name=${service}" --format '{{.Replicas}}' | head -n 1)
              update_state=$(docker service inspect "$service" --format '{{if .UpdateStatus}}{{.UpdateStatus.State}}{{else}}completed{{end}}' 2>/dev/null || echo missing)
              echo "${service}: replicas=${replicas:-missing}, update=${update_state}, attempt=${attempt}/36"

              if [ "$replicas" = "${expected}/${expected}" ] && [ "$update_state" = "completed" ]; then
                return 0
              fi

              if [ "$update_state" = "paused" ] || [ "$update_state" = "rollback_paused" ]; then
                diagnose_service "$service"
                return 1
              fi

              sleep 5
            done

            diagnose_service "$service"
            return 1
          }

          cd /itu-minitwit
          echo '${{ secrets.DOCKER_PASSWORD }}' | docker login -u '${{ secrets.DOCKER_USERNAME }}' --password-stdin
          export DOCKER_USERNAME='${{ secrets.DOCKER_USERNAME }}'
          export IMAGE_TAG='${{ env.IMAGE_TAG }}'
          export DB_URL='${{ secrets.DB_URL }}'
          docker compose down --remove-orphans || true
          docker stack deploy --compose-file docker-stack.yml --with-registry-auth --detach=true itu-minitwit

          wait_service itu-minitwit_itu-web-server 2
          wait_service itu-minitwit_itu-api-server 2
          REMOTE

      - name: Smoke test
        id: smoketest
        run: |
          for i in {1..20}; do
            curl --fail http://134.122.71.241:5001/health &&
            curl --fail http://134.122.71.241:8080/health &&
            exit 0
            sleep 2
          done
          echo "Health check failed."
          exit 1

      - name: Rollback on smoke test failure
        if: failure() && steps.smoketest.conclusion == 'failure'
        run: |
          ssh -o StrictHostKeyChecking=no root@134.122.71.241 "
            cd /itu-minitwit &&
            docker service rollback itu-minitwit_itu-web-server || true &&
            docker service rollback itu-minitwit_itu-api-server || true
          "
